# 上线教程：GitHub + Netlify 全自动部署

> 目标：把你的「冰某氚ICE-T」网站部署上线，之后每天自动更新新视频。
> 全程用浏览器操作，不需要装任何软件。预计 20-30 分钟。

---

## 第 1 步：创建 GitHub 仓库

1. 打开 https://github.com/new （如果你没登录，先登录）
2. 填写以下内容（其他不用动）：
   - **Repository name**：填 `bingmou-ice-t-site`
   - **Public / Private**：选 **Public**（免费版定时任务只对公开仓库免费）
   - **不要勾选**下方任何选项（不要勾 Add a README file、.gitignore、license！）
3. 点击绿色按钮 **Create repository**
4. 创建成功后会出现一个页面，先别关，进行第 2 步

---

## 第 2 步：上传网站文件

> 你的网站文件在本地这个文件夹：
> `C:\Users\Rika\lobsterai\project\bingmou-ice-t-repo`
> 里面有 index.html、assets 文件夹、.github 文件夹等。

### 2.1 先让隐藏文件夹显示出来（重要！）

1. 打开文件夹 `C:\Users\Rika\lobsterai\project\bingmou-ice-t-repo`
2. 点文件夹窗口顶部的 **查看** 菜单
3. 勾选 **隐藏的项目**（这样才看得到 `.github` 文件夹）
4. 确认能看到这些内容：`.github` 文件夹、`assets` 文件夹、`index.html`、`README.md`、`update_site.mjs` 等

### 2.2 上传

1. 回到 GitHub 刚创建的仓库页面
2. 点 **uploading an existing file** 链接（蓝色文字，在 "Quick setup" 区域）
   - 或者点 **Add file** 按钮 → **Upload files**
3. 现在进入上传页面（灰色虚线框）
4. 打开本地文件夹 `C:\Users\Rika\lobsterai\project\bingmou-ice-t-repo`
5. **按 Ctrl+A 全选** 文件夹里所有内容（包括 .github 和 assets 文件夹），**拖进**浏览器的灰色上传框
6. 等文件全部出现在列表里（应该能看到 .github 文件夹、assets 文件夹、index.html 等）
7. 页面底部 **Commit changes** 按钮 → 点它
8. 等待上传完成，回到仓库首页，确认能看到文件列表

---

## 第 3 步：手动触发第一次自动更新

1. 在仓库页面顶部点 **Actions** 标签
2. 左侧列表里应该能看到 **Auto Update Site**
3. 点它 → 右侧点 **Run workflow** 按钮 → 再点绿色的 **Run workflow**
4. 会开始运行，等 1-2 分钟，出现绿色 ✓ 就是成功
5. 如果出现红色 ✗，点进去看哪里红了，把错误信息发给我

---

## 第 4 步：部署到 Netlify（获得免费网址）

1. 打开 https://app.netlify.com
2. 点 **Sign up**，选择 **GitHub** 登录（授权即可，用你刚登录的 GitHub 账号）
3. 登录后点 **Add new site** → **Import an existing project**
4. 选择刚创建的仓库 **bingmou-ice-t-site**
5. 配置页面：
   - **Build command**：留空
   - **Publish directory**：填 `.`（一个英文句点）
   - 其他全部默认
6. 点 **Deploy bingmou-ice-t-site**
7. 等 1-2 分钟，显示 **Published** / **Site is live** 就成功了
8. 记下你的网址：形如 `https://xxxxxx.netlify.app`，点进去就能看到网站

### 可选：改一个好记的网址

1. Netlify 站点页 → **Site configuration**（或 Site settings）
2. 找到 **Change site name**
3. 改成 `bingmou-ice-t`，保存
4. 网址变为 `https://bingmou-ice-t.netlify.app`

---

## 之后会发生什么（全自动）

- 每天北京时间 **14:00**，GitHub 自动运行更新任务
- 任务会：访问 B 站 → 拉取 UP 主最新视频 → 下载新封面 → 更新网页文件 → 自动提交
- 提交后 Netlify 检测到变化 → 自动重新部署
- **你什么都不用做**，UP 主发新视频，第二天你的网站就是最新的

---

## 常见问题

**Q：上传时看不到 .github 文件夹？**
A：文件资源管理器 → 查看 → 勾选「隐藏的项目」。

**Q：Actions 显示红色 ✗？**
A：点进红色任务，看是哪一步报错，把报错文字复制发给我。

**Q：Netlify 部署后图片不显示？**
A：确认部署的是仓库根目录（Publish directory 填 `.`）。另外可以用 `index-standalone.html` 兜底：把它的内容复制成 `index.html` 再部署（单文件版图片全内嵌）。

**Q：网站打不开或很慢？**
A：Netlify 在国内偶尔慢，可以之后换 Cloudflare Pages，原理一样。

**Q：想换一个 UP 主？**
A：告诉我新 UP 主的 UID 或主页链接，我改好文件你再传一次即可。
